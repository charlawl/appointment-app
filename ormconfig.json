{
    "type": "postgres",
    "host": "localhost",
    "port": 5432,
    "username": "postgres",
    "password": "password",
    "database": "appointment_app",
    "synchronize": true,
    "entities": [
      "dist/**/*.entity.js", //NOTE: Type ORM requires the complied JS to run the migrations
      "src/**/*.entity.ts" //NOTE: this is for the e2e tests to run. Without this entities are unfindable by supertest...
    ],
    "migrations": [
      "dist/migrations/**/*.{ts,js}"
    ]
  }